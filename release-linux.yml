pool:
  vmImage: 'ubuntu-18.04'
  
variables:
  MAVEN_CACHE_FOLDER: $(Pipeline.Workspace)\.m2\repository
  MAVEN_OPTS: '-Dmaven.repo.local=$(MAVEN_CACHE_FOLDER)'

steps:
  - checkout: self
    clean: 'true'
    fetchDepth: '1'

  - task: Cache@2
    inputs:
      key: 'maven | "$(Agent.OS)"'
      path: $(MAVEN_CACHE_FOLDER)
    displayName: 'Cache Maven local repo'

  - script: |
      wget -qO - https://adoptopenjdk.jfrog.io/adoptopenjdk/api/gpg/key/public | sudo apt-key add -
      sudo add-apt-repository --yes https://adoptopenjdk.jfrog.io/adoptopenjdk/deb/
      sudo apt-get install adoptopenjdk-15-hotspot
      export JAVA_HOME=/usr/lib/jvm/adoptopenjdk-15-hotspot-amd64
      wget https://gluonhq.com/download/javafx-15-0-1-jmods-linux/ -O openjfx-15.0.1_linux-x64_bin-jmods.zip
      unzip openjfx-15.0.1_linux-x64_bin-jmods.zip
      export JAVAFX_JMODS=javafx-jmods-15.0.1
      mvn clean package --no-transfer-progress $(MAVEN_OPTS)
    displayName: 'Build deb package'
